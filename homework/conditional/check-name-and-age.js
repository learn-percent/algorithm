// There's a bar that only sell drink if you age is greater than 25, in execption
// if the name is Luigi, in which case the age drops down to 21; And if the name
// is Mario, you need to have at least 28

const checkNameAndAge = (name, age) => {}

export default checkNameAndAge